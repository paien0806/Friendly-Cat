import { EmptyInfoPipe } from './empty-info.pipe';

describe('EmptyInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
